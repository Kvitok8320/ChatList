# ChatList

ChatList — это Python-приложение с графическим интерфейсом на PyQt5, которое позволяет отправлять один и тот же промт в несколько нейросетей и сравнивать их ответы.

## Возможности

- 📝 Отправка промта в несколько нейросетей одновременно
- 💾 Сохранение промтов с тегами
- 📊 Сравнение ответов разных моделей в удобной таблице
- ✅ Выборочное сохранение результатов
- 🔍 Поиск и фильтрация по промтам, моделям и результатам
- 📤 Экспорт результатов в Markdown, JSON и TXT
- ⚙️ Управление моделями нейросетей
- 🔑 Поддержка различных API (OpenAI, DeepSeek, Groq, OpenRouter)

## Требования

- Python 3.11+
- SQLite (встроен в Python)
- PyQt5
- requests
- python-dotenv

## Установка

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` в корне проекта и добавьте ваши API-ключи:
```env
OPENAI_API_KEY=your_openai_api_key_here
DEEPSEEK_API_KEY=your_deepseek_api_key_here
GROQ_API_KEY=your_groq_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here
```

## Использование

### Запуск приложения

```bash
python main.py
```

### Основной рабочий процесс

1. **Ввод промта**: Введите текст промта в поле ввода или выберите сохраненный промт из выпадающего списка
2. **Отправка запроса**: Нажмите кнопку "Отправить запрос" - приложение отправит промт во все активные модели
3. **Просмотр результатов**: Результаты отображаются в таблице с чекбоксами для выбора
4. **Сохранение**: Выберите нужные результаты и нажмите "Сохранить выбранные результаты"
5. **Экспорт**: Используйте меню "Экспорт" для сохранения результатов в различных форматах

### Управление моделями

1. Откройте меню "Управление" → "Модели..."
2. Нажмите "Добавить модель" для добавления новой модели
3. Заполните поля:
   - **Название**: Имя модели (например, "GPT-4")
   - **API URL**: URL API для запросов
   - **API ID модели**: Идентификатор модели в API
   - **Переменная API-ключа**: Имя переменной из `.env` файла (например, "OPENAI_API_KEY")
   - **Тип API**: Выберите тип API (openai, deepseek, groq, openrouter)
   - **Активна**: Включить/выключить модель

### Управление промтами

1. Откройте меню "Управление" → "Промты..."
2. Используйте поиск для быстрого поиска промтов
3. Добавляйте, редактируйте и удаляйте промты
4. Добавляйте теги для организации промтов

### Просмотр сохраненных результатов

1. Откройте меню "Управление" → "Результаты..."
2. Просматривайте все сохраненные результаты
3. Используйте поиск для фильтрации
4. Удаляйте ненужные результаты

### Экспорт результатов

1. Выберите результаты в таблице (используйте чекбоксы)
2. Откройте меню "Экспорт" и выберите формат:
   - **Markdown** - для документов и отчетов
   - **JSON** - для программной обработки
   - **TXT** - простой текстовый формат

## Структура проекта

```
ChatList/
├── main.py              # Главный файл с GUI интерфейсом
├── db.py                # Модуль для работы с SQLite
├── models.py            # Модуль для работы с моделями нейросетей
├── network.py           # Модуль для HTTP-запросов к API
├── models_dialog.py     # Диалог управления моделями
├── prompts_dialog.py    # Диалог управления промтами
├── results_dialog.py    # Диалог просмотра результатов
├── export.py            # Модуль экспорта результатов
├── requirements.txt     # Зависимости проекта
├── .env                 # API-ключи (создается вручную)
├── chatlist.db          # База данных SQLite (создается автоматически)
└── chatlist.log         # Лог файл (создается автоматически)
```

## Примеры настройки моделей

### OpenAI
- **API URL**: `https://api.openai.com/v1/chat/completions`
- **API ID**: `gpt-4`, `gpt-3.5-turbo` и т.д.
- **Переменная API-ключа**: `OPENAI_API_KEY`

### DeepSeek
- **API URL**: `https://api.deepseek.com/v1/chat/completions`
- **API ID**: `deepseek-chat`, `deepseek-coder` и т.д.
- **Переменная API-ключа**: `DEEPSEEK_API_KEY`

### Groq
- **API URL**: `https://api.groq.com/openai/v1/chat/completions`
- **API ID**: `llama-3-70b-8192`, `mixtral-8x7b-32768` и т.д.
- **Переменная API-ключа**: `GROQ_API_KEY`

### OpenRouter
- **API URL**: `https://openrouter.ai/api/v1/chat/completions`
- **API ID**: `openai/gpt-4`, `anthropic/claude-3-opus` и т.д.
- **Переменная API-ключа**: `OPENROUTER_API_KEY`

## Логирование

Приложение создает файл `chatlist.log` с информацией о всех запросах к API и ошибках. Это помогает отслеживать проблемы и анализировать работу приложения.

## Создание исполняемого файла

Для создания исполняемого файла используйте PyInstaller:

```bash
pip install pyinstaller
python -m PyInstaller --onefile --windowed --name ChatListApp main.py
```

Исполняемый файл будет создан в папке `dist/`.

## Устранение неполадок

### Ошибка "API ключ не найден"
- Убедитесь, что файл `.env` существует в корне проекта
- Проверьте, что имя переменной в настройках модели совпадает с именем в `.env`
- Убедитесь, что в `.env` нет лишних пробелов

### Модели не отвечают
- Проверьте интернет-соединение
- Убедитесь, что API-ключ действителен
- Проверьте логи в файле `chatlist.log`
- Убедитесь, что модель активна в настройках

### Ошибки базы данных
- Удалите файл `chatlist.db` и перезапустите приложение
- База данных будет создана заново при следующем запуске

## Лицензия

См. файл LICENSE

## Автор

ChatList - инструмент для сравнения ответов нейросетей
