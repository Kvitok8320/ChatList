# 📦 Руководство по публикации ChatList

## Подготовка к релизу

### Шаг 1: Обновление версии

1. Откройте `version.py`
2. Обновите `__version__` (например, `"1.0.1"`)
3. Сохраните файл

### Шаг 2: Сборка исполняемого файла

```powershell
# Активируйте виртуальное окружение
venv\Scripts\Activate.ps1

# Установите зависимости (если нужно)
pip install -r requirements.txt

# Соберите исполняемый файл
python -m PyInstaller ChatListApp.spec --clean
```

Проверьте: `dist\ChatListApp-{version}.exe` должен быть создан.

### Шаг 3: Создание установщика

```powershell
# Сгенерируйте .iss файл с актуальной версией
python create_installer.py

# Создайте установщик
.\build_installer.ps1
# ИЛИ вручную через Inno Setup Compiler
```

Проверьте: `installer\ChatList-Setup-{version}.exe` должен быть создан.

### Шаг 4: Тестирование

- [ ] Запустите исполняемый файл - он должен работать
- [ ] Установите через установщик - установка должна пройти успешно
- [ ] Проверьте работу приложения после установки
- [ ] Проверьте ввод API ключей через интерфейс

## Публикация на GitHub Release

### Вариант 1: Ручная публикация

#### 1. Создание тега

```bash
# Убедитесь, что все изменения закоммичены
git add .
git commit -m "Release v{version}"

# Создайте тег
git tag -a v{version} -m "Release version {version}"

# Отправьте тег на GitHub
git push origin v{version}
```

#### 2. Создание Release на GitHub

1. Перейдите на https://github.com/yourusername/chatlist/releases
2. Нажмите **"Draft a new release"**
3. Выберите созданный тег `v{version}`
4. **Заголовок**: `ChatList v{version}`
5. **Описание**: Скопируйте и заполните шаблон из `RELEASE_NOTES_TEMPLATE.md`
6. В разделе **"Attach binaries"** загрузите:
   - `installer\ChatList-Setup-{version}.exe`
7. Нажмите **"Publish release"**

### Вариант 2: Автоматическая публикация (GitHub Actions)

Если настроен workflow (`.github/workflows/release.yml`):

1. Создайте и отправьте тег:
   ```bash
   git tag -a v{version} -m "Release version {version}"
   git push origin v{version}
   ```

2. GitHub Actions автоматически:
   - Соберет исполняемый файл
   - Создаст установщик
   - Создаст Release
   - Загрузит установщик

## Настройка GitHub Pages

### Шаг 1: Подготовка файлов

1. Убедитесь, что файл `docs/index.html` существует
2. Обновите ссылки в `docs/index.html`:
   - Замените `yourusername` на ваш GitHub username
   - Замените `chatlist` на название вашего репозитория

### Шаг 2: Включение GitHub Pages

1. Перейдите в **Settings** → **Pages**
2. **Source**: Выберите **"Deploy from a branch"**
3. **Branch**: Выберите **main** (или **gh-pages**)
4. **Folder**: Выберите **/docs**
5. Нажмите **Save**

### Шаг 3: Публикация

1. Закоммитьте и запушьте изменения:
   ```bash
   git add docs/
   git commit -m "Add landing page"
   git push origin main
   ```

2. GitHub автоматически опубликует сайт
3. Сайт будет доступен по адресу: `https://yourusername.github.io/chatlist/`

### Автоматическое обновление (GitHub Actions)

Если настроен workflow (`.github/workflows/pages.yml`), страница будет обновляться автоматически при изменении файлов в `docs/`.

## Чеклист перед релизом

### Подготовка
- [ ] Версия обновлена в `version.py`
- [ ] Все изменения закоммичены
- [ ] README.md актуален
- [ ] LICENSE файл присутствует

### Сборка
- [ ] Исполняемый файл собран (`dist\ChatListApp-{version}.exe`)
- [ ] Установщик создан (`installer\ChatList-Setup-{version}.exe`)
- [ ] Исполняемый файл протестирован
- [ ] Установщик протестирован

### Публикация
- [ ] Тег создан и отправлен на GitHub
- [ ] Release создан на GitHub
- [ ] Release notes заполнены
- [ ] Установщик загружен в Release
- [ ] GitHub Pages настроен (если нужно)

## Обновление документации

После релиза обновите:

1. **README.md** - если есть новые функции
2. **INSTRUCTIONS.md** - если изменился процесс использования
3. **TROUBLESHOOTING.md** - если были исправлены известные проблемы

## Примеры команд

```bash
# Полный цикл релиза
git add .
git commit -m "Prepare release v1.0.1"
git tag -a v1.0.1 -m "Release version 1.0.1"
git push origin main
git push origin v1.0.1

# После этого создайте Release на GitHub вручную или дождитесь автоматической сборки
```

## Полезные ссылки

- [GitHub Releases Documentation](https://docs.github.com/en/repositories/releasing-projects-on-github)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Semantic Versioning](https://semver.org/)

