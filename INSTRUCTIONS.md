# Инструкция по использованию ChatList

## Быстрый старт

### 1. Первоначальная настройка

1. **Создайте файл `.env`** в корне проекта со следующим содержимым:
```env
OPENAI_API_KEY=ваш_ключ_openai
DEEPSEEK_API_KEY=ваш_ключ_deepseek
GROQ_API_KEY=ваш_ключ_groq
OPENROUTER_API_KEY=ваш_ключ_openrouter
```

2. **Запустите приложение**:
```bash
python main.py
```

### 2. Настройка моделей

Перед первым использованием необходимо добавить модели нейросетей:

1. Откройте меню **"Управление"** → **"Модели..."**
2. Нажмите **"Добавить модель"**
3. Заполните форму:
   - **Название**: например, "GPT-4"
   - **API URL**: URL API (см. примеры ниже)
   - **API ID модели**: идентификатор модели
   - **Переменная API-ключа**: имя переменной из `.env` (например, `OPENAI_API_KEY`)
   - **Тип API**: выберите из списка (openai, deepseek, groq, openrouter)
   - **Активна**: включите чекбокс
4. Нажмите **"OK"**

### 3. Примеры настройки популярных моделей

#### OpenAI GPT-4
- **Название**: GPT-4
- **API URL**: `https://api.openai.com/v1/chat/completions`
- **API ID модели**: `gpt-4`
- **Переменная API-ключа**: `OPENAI_API_KEY`
- **Тип API**: `openai`

#### OpenAI GPT-3.5 Turbo
- **Название**: GPT-3.5 Turbo
- **API URL**: `https://api.openai.com/v1/chat/completions`
- **API ID модели**: `gpt-3.5-turbo`
- **Переменная API-ключа**: `OPENAI_API_KEY`
- **Тип API**: `openai`

#### DeepSeek Chat
- **Название**: DeepSeek Chat
- **API URL**: `https://api.deepseek.com/v1/chat/completions`
- **API ID модели**: `deepseek-chat`
- **Переменная API-ключа**: `DEEPSEEK_API_KEY`
- **Тип API**: `deepseek`

#### Groq Llama 3
- **Название**: Llama 3 70B
- **API URL**: `https://api.groq.com/openai/v1/chat/completions`
- **API ID модели**: `llama-3-70b-8192`
- **Переменная API-ключа**: `GROQ_API_KEY`
- **Тип API**: `groq`

#### OpenRouter (любая модель)
- **Название**: Claude 3 Opus
- **API URL**: `https://openrouter.ai/api/v1/chat/completions`
- **API ID модели**: `anthropic/claude-3-opus`
- **Переменная API-ключа**: `OPENROUTER_API_KEY`
- **Тип API**: `openrouter`

> 💡 **Совет**: Список доступных моделей на OpenRouter можно найти на https://openrouter.ai/models

## Основной рабочий процесс

### Шаг 1: Ввод промта

1. Введите текст промта в большое текстовое поле
2. Или выберите сохраненный промт из выпадающего списка "Выбрать промт"
3. Нажмите кнопку **"Выбрать"** для загрузки выбранного промта

### Шаг 2: Отправка запроса

1. Нажмите кнопку **"Отправить запрос"**
2. Дождитесь завершения запросов (появится индикатор "Отправка запросов...")
3. Результаты появятся в таблице ниже

### Шаг 3: Просмотр и выбор результатов

1. Просмотрите ответы всех моделей в таблице
2. Отметьте чекбоксами те результаты, которые хотите сохранить
3. Каждая строка содержит:
   - **Чекбокс** для выбора
   - **Название модели**
   - **Ответ модели**

### Шаг 4: Сохранение результатов

1. Отметьте нужные результаты чекбоксами
2. Нажмите **"Сохранить выбранные результаты"**
3. Результаты будут сохранены в базу данных

### Шаг 5: Экспорт (опционально)

1. Выберите результаты чекбоксами
2. Откройте меню **"Экспорт"**
3. Выберите формат:
   - **Markdown** - для документов
   - **JSON** - для программной обработки
   - **TXT** - простой текст
4. Укажите место сохранения файла

## Работа с промтами

### Сохранение промта

1. Введите текст промта
2. Нажмите **"Сохранить промт"**
3. В открывшемся окне:
   - Проверьте текст промта
   - Добавьте теги через запятую (например: "программирование, python, api")
4. Нажмите **"OK"**

### Управление промтами

1. Откройте меню **"Управление"** → **"Промты..."**
2. Используйте поиск для быстрого нахождения промтов
3. Доступные действия:
   - **Добавить промт** - создать новый
   - **Редактировать** - изменить существующий
   - **Удалить** - удалить промт
   - **Обновить** - обновить список

## Управление моделями

### Добавление новой модели

1. Меню **"Управление"** → **"Модели..."**
2. Нажмите **"Добавить модель"**
3. Заполните все поля формы
4. Нажмите **"OK"**

### Включение/выключение модели

1. Выберите модель в списке
2. Нажмите **"Включить/Выключить"**
3. Статус изменится (активные модели используются при отправке запросов)

### Редактирование модели

1. Выберите модель в списке
2. Нажмите **"Редактировать"**
3. Измените нужные поля
4. Нажмите **"OK"**

### Удаление модели

1. Выберите модель в списке
2. Нажмите **"Удалить"**
3. Подтвердите удаление

## Просмотр сохраненных результатов

1. Откройте меню **"Управление"** → **"Результаты..."**
2. Просмотрите все сохраненные результаты
3. Используйте поиск для фильтрации
4. Удаляйте ненужные результаты кнопкой **"Удалить"**

## Экспорт результатов

### Экспорт текущих результатов

1. После получения ответов от моделей
2. Отметьте нужные результаты чекбоксами
3. Нажмите **"Экспорт результатов"** или используйте меню **"Экспорт"**
4. Выберите формат и место сохранения

### Форматы экспорта

- **Markdown (.md)**: Удобен для документов, отчетов, блогов
- **JSON (.json)**: Для программной обработки, интеграции с другими инструментами
- **TXT (.txt)**: Простой текстовый формат для чтения

## Советы и рекомендации

### Оптимизация работы

1. **Сохраняйте часто используемые промты** - это ускорит работу
2. **Используйте теги** - для быстрого поиска промтов
3. **Отключайте неиспользуемые модели** - чтобы не тратить время на запросы
4. **Проверяйте логи** - файл `chatlist.log` содержит информацию о всех запросах

### Решение проблем

#### Модели не отвечают

1. Проверьте интернет-соединение
2. Убедитесь, что API-ключ действителен и указан в `.env`
3. Проверьте, что модель активна (чекбокс "Активна" включен)
4. Посмотрите логи в файле `chatlist.log`

#### Ошибка "API ключ не найден"

1. Проверьте файл `.env` в корне проекта
2. Убедитесь, что имя переменной в настройках модели совпадает с именем в `.env`
3. Убедитесь, что в `.env` нет лишних пробелов

#### Приложение не запускается

1. Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
2. Проверьте версию Python (требуется 3.11+)
3. Посмотрите сообщения об ошибках в консоли

### Безопасность

- **Никогда не коммитьте файл `.env`** в git - он уже в `.gitignore`
- **Не делитесь API-ключами** - они дают доступ к вашим аккаунтам
- **Регулярно проверяйте логи** на предмет подозрительной активности

## Горячие клавиши

- **Ctrl+Q** - выход из приложения (стандартная для PyQt)
- **Enter** в поле промта - переход на новую строку
- **Tab** - переход между элементами интерфейса

## Дополнительная информация

- База данных SQLite создается автоматически при первом запуске
- Логи сохраняются в файл `chatlist.log`
- Все настройки хранятся в базе данных
- Результаты можно экспортировать в любой момент

---

**Приятной работы с ChatList!** 🚀

